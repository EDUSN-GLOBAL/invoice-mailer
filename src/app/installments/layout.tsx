import React from 'react';

const InstallmentLayout = ({children}: {children: React.ReactNode}) => {
    return (
        <div>

            {children}
        </div>
    );
};

export default InstallmentLayout;
